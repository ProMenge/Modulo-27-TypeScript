"use strict";
const nameInput = document.getElementById('name');
const form = document.getElementById('form');
const sendButton = document.getElementById('btn-send');
sendButton === null || sendButton === void 0 ? void 0 : sendButton.addEventListener('click', (e) => {
});
//Embora o TypeScript tenha diversas funções, para o front-end em diversos casos, como na construção de uma landing page
//o trabalho pode ser demais para um resultado simples, o TypeScript tem uma utilização melhor para o backend
